# Mergington High School API - Environment Configuration
# Copy this file to .env and update with your actual credentials
# IMPORTANT: Never commit .env file with real credentials to version control

# ============================================================================
# Teacher Credentials (Initial Database Setup)
# ============================================================================
# These environment variables control the default teacher accounts created
# during database initialization. If not set, the system uses development
# defaults for local testing and development.

# First Teacher Account
TEACHER_1_USERNAME=ms_rodriguez
TEACHER_1_PASSWORD=SecurePass123
TEACHER_1_DISPLAY_NAME=Ms. Rodriguez

# Second Teacher Account
TEACHER_2_USERNAME=mr_smith
TEACHER_2_PASSWORD=TeacherPass456
TEACHER_2_DISPLAY_NAME=Mr. Smith

# ============================================================================
# Production Deployment Guidelines
# ============================================================================
# For production:
# 1. Set all TEACHER_* variables with strong, unique passwords
# 2. Use a secure secrets management system (AWS Secrets Manager, HashiCorp Vault, etc.)
# 3. Never store .env file in version control
# 4. Rotate passwords regularly
# 5. Use database migration tools to manage account lifecycle

# ============================================================================
# Development/Testing
# ============================================================================
# For local development, you can use the default values in .env.example
# or set custom values here. Database is idempotent - existing accounts
# won't be overwritten if already initialized.
